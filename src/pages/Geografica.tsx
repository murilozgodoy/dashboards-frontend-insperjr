import React from 'react';
import Layout from '../components/Layout';

const Geografica: React.FC = () => {
  return (
    <Layout title="Análise Geográfica" subtitle="Performance por região e bairros">
      <div style={{ background: 'white', borderRadius: 16, padding: 16, border: '1px solid #e2e8f0' }}>
        Em breve: top bairros, macro-regiões e mapa/alternativas.
      </div>
    </Layout>
  );
};

export default Geografica;


